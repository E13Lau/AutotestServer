
let deploy = function (name, version, url) {
    // https://github.com/ios-control/ios-deploy
    console.log(`Deploying ${name} ${version} to ${url}`);
    return `Deploying ${name} ${version} to ${url}`;
};

module.exports = deploy
